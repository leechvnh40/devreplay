# ADR 0001：显式 Agent 状态机

- 状态：Accepted
- 日期：2026-09-04

## 决策

复盘流程由持久化的显式阶段与操作状态驱动；模型调用只产生候选产物，阶段转换由应用服务校验，应用退出时运行中操作恢复为可重试错误。

## 原因与后果

显式状态让重启恢复、重试和完成门槛可确定测试，避免聊天历史被误当作业务状态。代价是每个新阶段都要维护迁移与恢复规则，但 Alpha 更重视可解释和不丢数据。
